---
stages:
  - 🚀 deploy

release:
  image: node:12-buster-slim
  stage: 🚀 deploy
  before_script:
    - npm install -g semantic-release @semantic-release/gitlab
  script:
    - semantic-release
  only:
    - master
    - branches